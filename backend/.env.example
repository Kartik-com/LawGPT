PORT=5000
CORS_ORIGIN=http://localhost:8080
NODE_ENV=development

# MongoDB Atlas Connection String (REQUIRED)
# Get from: https://cloud.mongodb.com/ → Connect → Drivers → Node.js
# IMPORTANT: Replace 'your-username', 'your-password', and 'cluster0.xxxxx' with your actual values
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/lawgpt?retryWrites=true&w=majority

# JWT Secret for Authentication (REQUIRED)
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANT: Use a minimum of 32 characters. Change this in production!
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-change-this-in-production

# Cloudinary Configuration (REQUIRED for file uploads)
# Get these from your Cloudinary dashboard: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Optional: Frontend URL (for CORS and password reset emails)
# FRONTEND_URL=http://localhost:8080
